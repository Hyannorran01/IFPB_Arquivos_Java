CREATE TABLE IF NOT EXISTS CAMISA (
    ID INT PRIMARY KEY AUTO_INCREMENT,
    NOME VARCHAR(255),
    DESCRICAO VARCHAR(500),
    QUANTIDADE INT,
    VALOR INT,
    ID_ADMIN_FK INT
    );



CREATE TABLE IF NOT EXISTS CLIENTE (
    CPF INT PRIMARY KEY AUTO_INCREMENT,
    NOME VARCHAR(255),
    EMAIL VARCHAR(500),
    SENHA INT
);

CREATE TABLE IF NOT EXISTS ADMINISTRADOR (
    ID_ADMIN INT PRIMARY KEY AUTO_INCREMENT,
    EMAIL VARCHAR(255) DEFAULT NULL,
    SENHA INT NOT NULL
);

--ALTER TABLE CAMISA
--ADD CONSTRAINT CAMISA_IBFK_1
--FOREIGN KEY (ID_ADMIN_FK) REFERENCES ADMINISTRADOR(ID_ADMIN);
